{
  "id": "transfer-document-ownership",
  "name": "Transfer Document Ownership (V6)",
  "version": "2.0.0",
  "category": "blockchain",
  "description": "Transfer document ownership to new address - IntegraDocumentRegistryV6",
  "integraId": 10,
  "defaults": {
    "chain": "polygon",
    "reason": ""
  },
  "metadata": {
    "tags": ["ownership-transfer", "v6", "document-registry"],
    "tier": 3,
    "category": "blockchain",
    "permissions": ["blockchain.document-registry.write"]
  },
  "workflow": {
    "steps": [
      {
        "id": "transfer-ownership",
        "name": "Transfer Document Ownership on Blockchain",
        "type": "activity",
        "action": "message",
        "timeout": "300s",
        "retryPolicy": {
          "initialInterval": "5s",
          "maximumAttempts": 5,
          "maximumInterval": "60s",
          "backoffCoefficient": 2
        },
        "actionParams": {
          "type": "blockchain",
          "chain": "polygon",
          "contractType": "document-registry",
          "contractVersion": "v6.0"
        },
        "continueOnFailure": false
      }
    ],
    "inputSchema": {
      "type": "object",
      "required": ["integraHash", "newOwner", "reason"],
      "properties": {
        "integraHash": {
          "type": "string",
          "pattern": "^0x[a-fA-F0-9]{64}$",
          "description": "Document to transfer"
        },
        "newOwner": {
          "type": "string",
          "pattern": "^0x[a-fA-F0-9]{40}$",
          "description": "New owner address"
        },
        "reason": {
          "type": "string",
          "description": "Audit trail reason for transfer"
        }
      }
    }
  },
  "blockchainSchema": {
    "method": "transferDocumentOwnership",
    "contract": "IntegraDocumentRegistry",
    "contractType": "document-registry",
    "parameters": {
      "integraHash": {"source": "input", "path": "integraHash"},
      "newOwner": {"source": "input", "path": "newOwner"},
      "reason": {"source": "input", "path": "reason"}
    }
  },
  "outputs": {
    "transactionHash": "${results.transfer-ownership.transactionHash}"
  }
}

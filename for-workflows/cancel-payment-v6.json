{
  "id": "cancel-payment-v6",
  "name": "Cancel Payment (V6)",
  "version": "2.0.0",
  "category": "blockchain",
  "description": "Cancel payment request - IntegraSignalV6",
  "integraId": 17,
  "defaults": {
    "chain": "polygon"
  },
  "metadata": {
    "tags": ["payment-cancellation", "v6", "signal"],
    "tier": 3,
    "category": "blockchain",
    "permissions": ["blockchain.signal.write"]
  },
  "workflow": {
    "steps": [
      {
        "id": "cancel-payment",
        "name": "Cancel Payment on Blockchain",
        "type": "activity",
        "action": "message",
        "timeout": "300s",
        "retryPolicy": {
          "initialInterval": "5s",
          "maximumAttempts": 5,
          "maximumInterval": "60s",
          "backoffCoefficient": 2
        },
        "actionParams": {
          "type": "blockchain",
          "chain": "polygon",
          "contractType": "signal",
          "contractVersion": "v6.0"
        },
        "continueOnFailure": false
      }
    ],
    "inputSchema": {
      "type": "object",
      "required": ["requestId"],
      "properties": {
        "requestId": {
          "type": "string",
          "pattern": "^0x[a-fA-F0-9]{64}$",
          "description": "Payment request identifier"
        }
      }
    }
  },
  "blockchainSchema": {
    "method": "cancelPayment",
    "contract": "IntegraSignal",
    "contractType": "signal",
    "parameters": {
      "requestId": {"source": "input", "path": "requestId"}
    }
  },
  "outputs": {
    "transactionHash": "${results.cancel-payment.transactionHash}"
  }
}
